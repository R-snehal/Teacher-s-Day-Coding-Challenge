#include <bits/stdc++.h>
using namespace std;
int main() {
    set<int> unique_colors;
    for (int i = 0; i < 4; ++i) {
        int s;
        cin >> s;
        unique_colors.insert(s);
    }
    cout << 4 - unique_colors.size() << endl;
    
    return 0;
}
