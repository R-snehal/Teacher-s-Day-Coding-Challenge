#include <iostream>
using namespace std;
int main() {
    int t;
    cin >> t;
    while (t--) {
        int n, m;
        cin >> n >> m;
        int total_squares = n * m;
        int min_lanterns = (total_squares + 1) / 2;
        cout << min_lanterns << endl;
    }
 
    return 0;
}
